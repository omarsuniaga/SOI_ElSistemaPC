/**
 * Test Fixtures & Demo Fallback Data
 * MOVED from production runtime to fixtures.
 * STRICT RULE: Production never uses these as authoritative data.
 */

export * from '../../data/initialData';
