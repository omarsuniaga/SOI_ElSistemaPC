strict_tdd: true
context: |
  sistema-academico-pwa is a Node.js/TypeScript ESM project.
  Detected markers: package.json, npm package manager, GitHub Actions.
  Package managers: npm.
  Additional evidence: Test-like files detected (54); examples: .pi\agents\sdd-spec.md, docs\superpowers\specs\2026-05-10-ruta-gamificada-spec.md, src\__tests__\notification-system.integration.test.js, src\components\__tests__\ErrorBoundary.test.js, src\lib\__tests__\fuzzyMatch.test.js.
  Primary test command: npm run test:run.
  Unit tests: Vitest (npm run test:run).
  Integration tests: none.
  E2E tests: none.
rules:
  proposal:
    require_problem_statement: true
  spec:
    require_acceptance_criteria: true
  design:
    require_tradeoffs: true
  tasks:
    protect_review_workload: true
  apply:
    test_command: "npm run test:run"
  verify:
    test_command: "npm run test:run"
testing:
  detected: "2026-05-17"
  runner:
    command: "npm run test:run"
    framework: "Vitest"
  layers:
    unit: "Vitest"
    integration: ""
    e2e: ""
  commands:
    unit:
      - scope: "."
        command: "npm run test:run"
        framework: "Vitest"
    integration:
      []
    e2e:
      []
  coverage:
    command: ""
    commands:
      []
quality:
  lint: ""
  lint_commands:
    []
  typecheck: ""
  typecheck_commands:
    []
  format: ""
  format_commands:
    []
