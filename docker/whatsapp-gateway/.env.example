# Copia este archivo a ".env" en esta misma carpeta y define un valor real.
# docker compose lo carga automáticamente. NUNCA commitear el ".env" real.

# Genera una clave larga y aleatoria, ej: openssl rand -hex 32
EVOLUTION_API_KEY=changeme_genera_una_clave_larga_aleatoria
