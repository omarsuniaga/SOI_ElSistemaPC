/**
 * eventProjectManagerModal.js — Modal placeholder para gestión de macro-eventos WBS.
 */

export function abrirEventProjectManagerModal(eventoId) {
  console.log('[eventProjectManagerModal] Abriendo modal para evento:', eventoId)
  alert(`Gestor de Proyecto para evento ID: ${eventoId}`)
}
