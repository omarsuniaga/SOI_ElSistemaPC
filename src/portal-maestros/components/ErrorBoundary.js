/**
 * Re-exports from the canonical ErrorBoundary in src/components/.
 * Do not duplicate logic here — all changes go to the source.
 */
export { renderErrorBoundary, ErrorBoundary } from '../../components/ErrorBoundary.js'
